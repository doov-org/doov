    @SuppressWarnings("unchecked")
    private <T> T get(${field.id.type} fieldId) {
        try {
            switch (fieldId) {
${switch.content}
                default:
                    throw new IllegalArgumentException("unsupported field " + fieldId);
            }
        } catch (IndexOutOfBoundsException e) {
        	return null;
        }
    }

