            @Override
            public BiConsumer<${target.model.class.name}, ${field.type}> consumer() {
                return (model, value) -> {
${lazy.init}                    model.${setter.path};
                };
            }
