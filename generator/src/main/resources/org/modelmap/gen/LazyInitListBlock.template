		if (projet.${partial.path} == null)
			projet.${partial.path.init};${list.content.as.null}
		if (projet.${partial.path}.size() < ${position})
			projet.${partial.path}.add(${index}, new ${target.type}());
		if (projet.${partial.path}.get(${index}) == null)
			projet.${partial.path}.set(${index}, new ${target.type}());
