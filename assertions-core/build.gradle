ext.pomProjectName = 'dOOv Assertions Core'

dependencies {
    api project(':doov-ts-parser')
    api group: 'org.assertj', name: 'assertj-core', version: assertjVersion
  	api group: 'org.jsoup', name: 'jsoup', version: jsoupVersion
    api group: 'com.atlassian.commonmark', name: 'commonmark', version: commonmarkVersion
}

if (isReleaseVersion) {
    javadoc.destinationDir = file("${rootProject.projectDir}/docs/site/assertions/apidocs")
}
