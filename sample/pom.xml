<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.modelmap</groupId>
        <artifactId>modelmap-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>modelmap-sample</artifactId>
    <name>ModelMap Samples</name>

    <profiles>
        <profile>
            <id>generate</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.modelmap</groupId>
                        <artifactId>modelmap-generator</artifactId>
                        <version>${project.version}</version>
                        <executions>
                            <execution>
                                <id>model-map-gen</id>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                                <phase>generate-sources</phase>
                                <configuration>
                                    <outputDirectory>${basedir}/src/generated/java</outputDirectory>
                                    <sourceClasses>
                                        <sourceClass>net.courtanet.tm.auto.ProjetAuto</sourceClass>
                                    </sourceClasses>
                                    <fieldClasses>
                                        <fieldClass>net.courtanet.ui.auto.ETunnelAuto</fieldClass>
                                    </fieldClasses>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <dependencies>
        <dependency>
            <groupId>org.modelmap</groupId>
            <artifactId>modelmap-core</artifactId>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>